spring.application.name=olx_backend
# MySQL database connection URL (replace 'your_database' with your actual DB name)
spring.datasource.url=jdbc:mysql://localhost:3306/upcyclex

# MySQL database username
spring.datasource.username=D5_89499_vaishanav

# MySQL database password
spring.datasource.password=manager

# JDBC driver class name for MySQL
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA configuration: auto update database schema (use with caution in production)
spring.jpa.hibernate.ddl-auto=update

# Show SQL statements in logs (useful for debugging)
spring.jpa.show-sql=true

# Set the dialect Hibernate should use for MySQL 8
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect


